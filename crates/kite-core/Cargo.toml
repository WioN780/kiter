[package]
name = "kite-core"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = { version = "0.24", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
noise = "0.8"
parry3d-f64 = "0.15"
rayon = "1.8"

[dev-dependencies]
approx = "0.5"
toml = "0.8"

[features]
default = []
reference-mode = []
